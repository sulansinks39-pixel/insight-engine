import { createFileRoute } from "@tanstack/react-router";
import { useServerFn } from "@tanstack/react-start";
import { useEffect, useState, type FormEvent } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { getProfile, updateProfile } from "@/lib/history.functions";
export const Route=createFileRoute("/app/settings")({head:()=>({meta:[{title:"Settings — Evidence"},{name:"description",content:"Manage your Evidence profile and research preferences."},{property:"og:title",content:"Settings — Evidence"},{property:"og:description",content:"Manage your Evidence profile and research preferences."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:SettingsPage});
function SettingsPage(){const get=useServerFn(getProfile);const save=useServerFn(updateProfile);const[name,setName]=useState("");const[avatar,setAvatar]=useState("");const[msg,setMsg]=useState("");useEffect(()=>{get({}).then(p=>{setName(p.display_name??"");setAvatar(p.avatar_url??"")}).catch(()=>setMsg("Sign in to update your profile."))},[get]);const submit=async(e:FormEvent)=>{e.preventDefault();await save({data:{display_name:name,avatar_url:avatar}});setMsg("Profile saved.")};return <div className="mx-auto max-w-3xl px-5 py-10"><p className="meta-label text-primary">Account</p><h1 className="mt-3 font-serif text-4xl">Settings</h1><form onSubmit={submit} className="mt-10 max-w-lg space-y-6 border-t pt-8"><label className="block"><span className="meta-label mb-2 block">Display name</span><Input value={name} onChange={e=>setName(e.target.value)}/></label><label className="block"><span className="meta-label mb-2 block">Avatar image URL</span><Input value={avatar} onChange={e=>setAvatar(e.target.value)} placeholder="https://…"/></label><div className="border-y py-5"><p className="font-medium">Research preferences</p><label className="mt-4 flex items-center gap-3 text-sm"><input type="checkbox" defaultChecked/>Prioritize peer-reviewed literature</label><label className="mt-3 flex items-center gap-3 text-sm"><input type="checkbox" defaultChecked/>Show exact evidence passages</label></div><Button>Save settings</Button>{msg&&<p className="text-sm text-muted-foreground">{msg}</p>}</form></div>}
