import { createFileRoute, Link } from "@tanstack/react-router";
import { useServerFn } from "@tanstack/react-start";
import { Clock3 } from "lucide-react";
import { useEffect, useState } from "react";
import { listConversations, type ConversationSummary } from "@/lib/history.functions";
export const Route=createFileRoute("/app/history")({head:()=>({meta:[{title:"Search history — Evidence"},{name:"description",content:"Reopen past research questions and cited answers."},{property:"og:title",content:"Search history — Evidence"},{property:"og:description",content:"Reopen past research questions and cited answers."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:HistoryPage});
function HistoryPage(){const fetcher=useServerFn(listConversations);const[items,setItems]=useState<ConversationSummary[]>([]);const[error,setError]=useState("");useEffect(()=>{fetcher({}).then(setItems).catch(()=>setError("Sign in to view your research history."))},[fetcher]);return <div className="mx-auto max-w-4xl px-5 py-10"><p className="meta-label text-primary">Saved research</p><h1 className="mt-3 font-serif text-4xl">History</h1><div className="mt-8 divide-y border-y">{items.map(x=><Link key={x.id} to="/app/results/$id" params={{id:x.id}} className="flex items-center gap-4 py-5 hover:text-primary"><Clock3 className="size-4"/><div><p className="font-serif text-lg">{x.title}</p><p className="mt-1 text-xs text-muted-foreground">{new Date(x.updated_at).toLocaleDateString()}</p></div></Link>)}{!items.length&&<p className="py-10 text-sm text-muted-foreground">{error||"Your completed searches will appear here."}</p>}</div></div>}
